{% extends '@SonataAdmin/CRUD/base_show_field.html.twig' %}

{% block field %}
    {% if value %}
        <img src="{{ s3_web_url }}images/{{ value }}" style="max-height:256px;">
    {% else %}
        No image uploaded
    {% endif %}
{% endblock %}